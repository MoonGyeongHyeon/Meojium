<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:scaleType="center"
        android:src="@drawable/ic_museum" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/textview_navigation_nickname"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:textSize="@dimen/large_text_size"
            tools:text="@string/navigation_nickname" />

        <ImageView
            android:id="@+id/imageview_navigation_change_nickname"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="end"
            android:padding="8dp"
            android:layout_marginBottom="16dp"
            android:src="@drawable/ic_change_nickname"
            android:clickable="true"
            android:background="@drawable/ripple_button_click"/>

    </FrameLayout>

</LinearLayout>